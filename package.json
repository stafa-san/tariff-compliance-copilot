{
  "name": "tariff-compliance-copilot",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter tariff-web dev",
    "dev:web": "pnpm --filter tariff-web dev",
    "dev:mobile": "cd apps/mobile && npx expo start",
    "build": "pnpm --filter tariff-web build",
    "build:web": "pnpm --filter tariff-web build",
    "lint": "pnpm --filter tariff-web lint",
    "sync-hts": "npx tsx scripts/sync-hts-data.ts",
    "ios": "cd apps/mobile && npx expo run:ios",
    "android": "cd apps/mobile && npx expo run:android"
  }
}
